function update_subcat() {
    var select = document.getElementById('subcategory-sort');
    var value = select.options[select.selectedIndex].value;

    console.log(value);
    switch (value) {
        
    }

}